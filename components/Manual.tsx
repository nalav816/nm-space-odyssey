import SectionCard from "./SectionCard"

export default function Manual({className} : {className?: string}){
    return (
        <SectionCard className = {className} sectionName = "How To Play"/>
    )
}